set $mod Mod4
set $left h
set $down j
set $up k
set $right l

set $term footclient
set $menu wofi

font pango:Hack 12

output * bg #000000 solid_color

xwayland disable

input "1133:50184:Logitech USB Trackball" {
    scroll_method on_button_down
    scroll_button button8
    middle_emulation enabled
}

input * {
    xkb_layout "us,ru"
    xkb_options "grp:caps_toggle"
}

default_border none

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod normal

bindsym --to-code {
    # start a terminal
    $mod+Return exec $term
    # kill focused window
    $mod+Shift+q kill
    # move workspace to another output
    $mod+o move workspace to output left

    # start wofi
    $mod+f exec $menu

    # change focus
    $mod+j focus left
    $mod+k focus down
    $mod+l focus up
    $mod+semicolon focus right

    # move between workspaces
    $mod+Right workspace next
    $mod+Leftt workspace prev

    # move focused window
    $mod+Shift+j move left
    $mod+Shift+k move down
    $mod+Shift+l move up
    $mod+Shift+colon move right

    # alternatively, you can use the cursor keys:
    $mod+Shift+Left move left
    $mod+Shift+Down move down
    $mod+Shift+Up move up
    $mod+Shift+Right move right

    # split in horizontal orientation
    $mod+h split h

    # split in vertical orientation
    $mod+v split v

    # enter fullscreen mode for the focused container
    $mod+F11 fullscreen toggle

    # toggle tiling / floating
    $mod+Shift+space floating toggle

    # change focus between tiling / floating windows
    $mod+space focus mode_toggle

    # focus urgent
    $mod+u [urgent=latest] focus

    # switch to workspace
    $mod+1 workspace $ws1
    $mod+2 workspace $ws2
    $mod+3 workspace $ws3
    $mod+4 workspace $ws4
    $mod+5 workspace $ws5
    $mod+6 workspace $ws6
    $mod+7 workspace $ws7
    $mod+8 workspace $ws8
    $mod+9 workspace $ws9
    $mod+0 workspace $ws10
    $mod+q workspace money
    $mod+w workspace game
    $mod+e workspace etc
    $mod+a workspace debug
    $mod+s workspace subs
    $mod+d workspace dl
    $mod+z workspace deanon
    $mod+x workspace map
    $mod+c workspace config

    # move focused container to workspace
    $mod+Shift+1 move container to workspace $ws1
    $mod+Shift+2 move container to workspace $ws2
    $mod+Shift+3 move container to workspace $ws3
    $mod+Shift+4 move container to workspace $ws4
    $mod+Shift+5 move container to workspace $ws5
    $mod+Shift+6 move container to workspace $ws6
    $mod+Shift+7 move container to workspace $ws7
    $mod+Shift+8 move container to workspace $ws8
    $mod+Shift+9 move container to workspace $ws9
    $mod+Shift+0 move container to workspace $ws10
    $mod+Shift+w move container to workspace game
    $mod+Shift+e move container to workspace etc
    $mod+Shift+a move container to workspace debug
    $mod+Shift+s move container to workspace subs
    $mod+Shift+d move container to workspace dl
    $mod+Shift+z move container to workspace deanon
    $mod+Shift+x move container to workspace map
    $mod+Shift+c move container to workspace config

    # reload the configuration file
    $mod+Ctrl+c reload
    # restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
    $mod+Ctrl+r restart
    # exit i3 (logs you out of your X session)
    $mod+Ctrl+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

    $mod+r mode "resize"
    $mod+F1 exec --no-startup-id loginctl lock-session
    $mod+F2 exec --no-startup-id systemctl suspend
    $mod+F4 exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle
    XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle
    XF86Tools exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle
    XF86MonBrightnessUp exec --no-startup-id brightnessctl s 10%+
    XF86MonBrightnessDown exec --no-startup-id brightnessctl s 10%-
    XF86ScreenSaver exec --no-startup-id loginctl lock-session
    XF86Explorer exec sensible-browser
    Print exec --no-startup-id grim -g "$(slurp)" - | wl-copy
    $mod+Pause mode "$mode_system"
}

bindsym --locked {
    XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +1000
    XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -1000
    XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle
    XF86AudioNext exec --no-startup-id playerctl next
    XF86AudioPrev exec --no-startup-id playerctl previous
    XF86AudioPlay exec --no-startup-id playerctl play-pause
    XF86AudioPause exec --no-startup-id playerctl play-pause
    XF86AudioStop exec --no-startup-id playerctl stop
}

set $ws1 "www"
set $ws2 "code"
set $ws3 "term"
set $ws4 "music"
set $ws5 "fm"
set $ws6 "im"
set $ws7 "mail"
set $ws8 "edit"
set $ws9 "pass"
set $ws10 "view"
set $ws11 "money"
set $ws12 "game"
set $ws13 "etc"
set $ws14 "debug"
set $ws15 "subs"
set $ws16 "dl"
set $ws17 "deanon"
set $ws18 "map"
set $ws19 "config"

set $nonprimary DVI-D-1 DP-1-2

workspace www output primary
workspace code output primary
workspace term output $nonprimary primary
workspace music output $nonprimary primary
workspace fm output $nonprimary primary
workspace im output $nonprimary primary
workspace mail output $nonprimary primary
workspace edit output primary
workspace pass output $nonprimary primary
workspace view output primary
workspace money output $nonprimary primary
workspace config output $nonprimary primary
workspace etc output $nonprimary primary
workspace debug output $nonprimary primary
workspace subs output $nonprimary primary
workspace dl output $nonprimary primary
workspace deanon output primary
workspace map output primary
workspace game output primary

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

mode "$mode_system" {
    bindsym l exec --no-startup-id loginctl lock-session, mode "default"
    bindsym e exec --no-startup-id i3-msg exit, mode "default"
    bindsym s exec --no-startup-id systemctl suspend, mode "default"
    bindsym r exec --no-startup-id systemctl reboot, mode "default"
    bindsym Ctrl+s exec --no-startup-id systemctl poweroff -i, mode "default"

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}


for_window [class="^.*"] border pixel 1
assign [app_id="firefox-esr"] www
for_window [app_id="VSCodium"] move container to workspace code
for_window [app_id="foot"] move container to workspace term
for_window [app_id="pms"] move container to workspace music
for_window [app_id="^(spacefm|pcmanfm)$"] move container to workspace fm
for_window [app_id="nnn"] move container to workspace fm
for_window [app_id="telegramdesktop"] move container to workspace im
for_window [app_id="slack-term"] move container to workspace im
for_window [app_id="thunderbird"] move container to workspace mail
for_window [app_id="^(gimp|inkscape|libreoffice|simple-scan|audacity|org-openstreetmap-josm-.*|zim)$"] move container to workspace edit, focus
for_window [app_id="org.keepassxc.KeePassXC"] move container to workspace pass
for_window [app_id="^(mpv|sxiv|imvr|zathura)$"] move container to workspace view, focus
for_window [app_id="^(uget-gtk|Uget-gtk|ariang-native)$"] move container to workspace dl
for_window [app_id="aria2p"] move container to workspace dl
for_window [app_id="^(Wireshark|Devtools|restscope|firefox-dev|gucharmap)$"] move container to workspace debug
for_window [app_id="firefox-dev"] move container to workspace debug
for_window [title="^DevTools - .*$" window_role="pop-up" app_id="^((B|b)rave-browser|(C|c)hromium)$"] move container to workspace debug, floating disable
for_window [app_id="FreeTube"] move container to workspace subs, focus
for_window [app_id="io.github.rinigus.PureMaps"] move container to workspace map, focus
for_window [app_id="^(pavucontrol|qt5ct|nm-connection-editor|system-config-printer|osmscout-server|virt-manager)$"] move container to workspace config
for_window [app_id="^(Wasabi Wallet|electrum|simpleos|electrum-dash|electron-cash|mycrypto|monero-wallet-gui|monero-core|bisq.desktop.app.BisqApp)$"] move container to workspace money
for_window [app_id="^firefox-deanon$"] move container to workspace deanon
for_window [app_id="^(Tor|i2p) Browser$"] move container to workspace www, floating enable
for_window [app_id="^(pinentry|Gsimplecal)$"] floating enable, focus
for_window [window_role="pop-up"] floating enable
for_window [window_role="bubble"] floating enable
for_window [window_role="task_dialog"] floating enable
for_window [window_role="Preferences"] floating enable
for_window [window_type="dialog"] floating enable
for_window [window_type="menu"] floating enable
for_window [window_role="About"] floating enable
for_window [class="xdg-desktop-portal-kde"] floating enable
for_window [class="xdg-desktop-portal-gtk"] floating enable

# colour of border, background, text, indicator, and child_border
client.focused              #2f2f2f #000000 #2f2f2f #2f2f2f #2f2f2f
client.focused_inactive     #000000 #000000 #2f2f2f #000000 #000000
client.unfocused            #000000 #000000 #2f2f2f #000000 #000000
client.urgent               #bf616a #000000 #2f2f2f #bf616a #000000
client.placeholder          #000000 #000000 #2f2f2f #000000 #000000
client.background           #000000

floating_minimum_size 75 x 50
focus_on_window_activation urgent
hide_edge_borders none
focus_follows_mouse yes
show_marks no

exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ mute
exec_always --no-startup-id pidof pms > /dev/null && exit 0 || footclient --app-id=pms pms
exec_always --no-startup-id pidof nnn > /dev/null && exit 0 || footclient --app-id=nnn nnn
exec --no-startup-id $term
exec swayidle -w \
    timeout 250 'swaymsg "output * dpms off"' resume 'swaymsg "output * dpms on"' \
    timeout 240 'swaylock' \
    before-sleep 'swaylock'

include /etc/sway/config.d/*
exec_always "systemctl --user start sway-session.target"
