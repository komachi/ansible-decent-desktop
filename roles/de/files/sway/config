set $mod Mod4
set $left h
set $down j
set $up k
set $right l

set $term footclient
set $menu wofi

set $ws1 1 # "www"
set $ws2 2 # "code"
set $ws3 3 # "term"
set $ws4 4 # "music"
set $ws5 5 # "fm"
set $ws6 6 # "im"
set $ws7 7 # "mail"
set $ws8 8 # "edit"
set $ws9 9 # "pass"
set $ws10 10 # "view"
set $ws11 11 # "money"
set $ws12 12 # "game"
set $ws13 13 # "etc"
set $ws14 14 # "debug"
set $ws15 15 # "subs"
set $ws16 16 # "dl"
set $ws17 17 # "deanon"
set $ws18 18 # "map"
set $ws19 19 # "config"

set $nonprimary DVI-D-1 DP-2

font pango:Hack 12

output * bg #000000 solid_color

# xwayland disable

input "1133:50184:Logitech USB Trackball" {
    scroll_method on_button_down
    scroll_button button8
    middle_emulation enabled
}

input * {
    xkb_layout "us,ru"
    xkb_options "grp:caps_toggle"
}

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod normal

bindsym --to-code {
    # start a terminal
    $mod+Return exec $term
    # kill focused window
    $mod+Shift+q kill
    # move workspace to another output
    $mod+o move workspace to output left

    # start wofi
    $mod+f exec $menu

    # change focus
    $mod+j focus left
    $mod+k focus down
    $mod+l focus up
    $mod+semicolon focus right

    # move between workspaces
    $mod+Right workspace next
    $mod+Left workspace prev

    # move focused window
    $mod+Shift+j move left
    $mod+Shift+k move down
    $mod+Shift+l move up
    $mod+Shift+colon move right

    # alternatively, you can use the cursor keys:
    $mod+Shift+Left move left
    $mod+Shift+Down move down
    $mod+Shift+Up move up
    $mod+Shift+Right move right

    # split in horizontal orientation
    $mod+h split h

    # split in vertical orientation
    $mod+v split v

    # enter fullscreen mode for the focused container
    $mod+F11 fullscreen toggle

    # toggle tiling / floating
    $mod+Shift+space floating toggle

    # change focus between tiling / floating windows
    $mod+space focus mode_toggle

    # focus urgent
    $mod+u [urgent=latest] focus

    # switch to workspace
    $mod+1 workspace $ws1
    $mod+2 workspace $ws2
    $mod+3 workspace $ws3
    $mod+4 workspace $ws4
    $mod+5 workspace $ws5
    $mod+6 workspace $ws6
    $mod+7 workspace $ws7
    $mod+8 workspace $ws8
    $mod+9 workspace $ws9
    $mod+0 workspace $ws10
    $mod+q workspace $ws11
    $mod+w workspace $ws12
    $mod+e workspace $ws13
    $mod+a workspace $ws14
    $mod+s workspace $ws15
    $mod+d workspace $ws16
    $mod+z workspace $ws17
    $mod+x workspace $ws18
    $mod+c workspace $ws19

    # move focused container to workspace
    $mod+Shift+1 move container to workspace $ws1
    $mod+Shift+2 move container to workspace $ws2
    $mod+Shift+3 move container to workspace $ws3
    $mod+Shift+4 move container to workspace $ws4
    $mod+Shift+5 move container to workspace $ws5
    $mod+Shift+6 move container to workspace $ws6
    $mod+Shift+7 move container to workspace $ws7
    $mod+Shift+8 move container to workspace $ws8
    $mod+Shift+9 move container to workspace $ws9
    $mod+Shift+0 move container to workspace $ws10
    $mod+Shift+w move container to workspace $ws12
    $mod+Shift+e move container to workspace $ws13
    $mod+Shift+a move container to workspace $ws14
    $mod+Shift+s move container to workspace $ws15
    $mod+Shift+d move container to workspace $ws16
    $mod+Shift+z move container to workspace $ws17
    $mod+Shift+x move container to workspace $ws18
    $mod+Shift+c move container to workspace $ws19

    # reload the configuration file
    $mod+Ctrl+c reload
    # restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
    $mod+Ctrl+r restart
    # exit i3 (logs you out of your X session)
    $mod+Ctrl+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

    $mod+r mode "resize"
    $mod+F1 exec --no-startup-id loginctl lock-session
    $mod+F2 exec --no-startup-id systemctl suspend
    $mod+F4 exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle
    XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle
    XF86Tools exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle
    XF86MonBrightnessUp exec --no-startup-id brightnessctl s 10%+
    XF86MonBrightnessDown exec --no-startup-id brightnessctl s 10%-
    XF86ScreenSaver exec --no-startup-id loginctl lock-session
    XF86Explorer exec sensible-browser
    Print exec --no-startup-id grim -g "$(slurp)" - | wl-copy
    $mod+Pause mode "$mode_system"
}

bindsym --locked {
    XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +1000
    XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -1000
    XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle
    XF86AudioNext exec --no-startup-id playerctl next
    XF86AudioPrev exec --no-startup-id playerctl previous
    XF86AudioPlay exec --no-startup-id playerctl play-pause
    XF86AudioPause exec --no-startup-id playerctl play-pause
    XF86AudioStop exec --no-startup-id playerctl stop
}


workspace $ws1 output primary
workspace $ws2 output primary
workspace $ws3 output $nonprimary primary
workspace $ws4 output $nonprimary primary
workspace $ws5 output $nonprimary primary
workspace $ws6 output $nonprimary primary
workspace $ws7 output $nonprimary primary
workspace $ws8 output primary
workspace $ws9 output $nonprimary primary
workspace $ws10 output primary
workspace $ws11 output $nonprimary primary
workspace $ws12 output primary
workspace $ws13 output $nonprimary primary
workspace $ws14 output $nonprimary primary
workspace $ws15 output $nonprimary primary
workspace $ws16 output $nonprimary primary
workspace $ws17 output primary
workspace $ws18 output primary
workspace $ws19 output $nonprimary primary

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

mode "$mode_system" {
    bindsym l exec --no-startup-id loginctl lock-session, mode "default"
    bindsym e exec --no-startup-id i3-msg exit, mode "default"
    bindsym s exec --no-startup-id systemctl suspend, mode "default"
    bindsym r exec --no-startup-id systemctl reboot, mode "default"
    bindsym Ctrl+s exec --no-startup-id systemctl poweroff -i, mode "default"

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

assign [app_id="firefox-esr"] $ws1
for_window [app_id="VSCodium"] move container to workspace $ws2
for_window [app_id="foot"] move container to workspace $ws3
for_window [app_id="pms"] move container to workspace $ws4
for_window [app_id="^(spacefm|pcmanfm)$"] move container to workspace $ws5
for_window [app_id="nnn"] move container to workspace $ws5
for_window [app_id="telegramdesktop"] move container to workspace $ws6
for_window [app_id="slack-term"] move container to workspace $ws6
for_window [app_id="thunderbird"] move container to workspace $ws7
for_window [app_id="^(gimp|inkscape|libreoffice|simple-scan|audacity|org-openstreetmap-josm-.*|zim)$"] move container to workspace $ws8, focus
for_window [app_id="org.keepassxc.KeePassXC"] move container to workspace $ws9
for_window [app_id="^(mpv|sxiv|imvr|zathura)$"] move container to workspace $ws10, focus
for_window [app_id="^(uget-gtk|Uget-gtk|ariang-native)$"] move container to workspace $ws16
for_window [app_id="aria2p"] move container to workspace $ws16
for_window [app_id="^(Wireshark|Devtools|restscope|firefox-dev|gucharmap)$"] move container to workspace $ws14
for_window [app_id="firefox-dev"] move container to workspace $ws14
for_window [title="^DevTools - .*$" window_role="pop-up" app_id="^((B|b)rave-browser|(C|c)hromium)$"] move container to workspace $ws14, floating disable
for_window [app_id="FreeTube"] move container to workspace $ws15, focus
for_window [app_id="io.github.rinigus.PureMaps"] move container to workspace $ws18, focus
for_window [app_id="^(pavucontrol|qt5ct|nm-connection-editor|system-config-printer|osmscout-server|virt-manager)$"] move container to workspace $ws19
for_window [app_id="^(Wasabi Wallet|electrum|simpleos|electrum-dash|electron-cash|mycrypto|monero-wallet-gui|monero-core|bisq.desktop.app.BisqApp)$"] move container to workspace $ws11
for_window [app_id="^firefox-deanon$"] move container to workspace $ws17
for_window [app_id="^(Tor|i2p) Browser$"] move container to workspace $ws1, floating enable
for_window [app_id="^(pinentry|Gsimplecal)$"] floating enable, focus
for_window [window_role="pop-up"] floating enable
for_window [window_role="bubble"] floating enable
for_window [window_role="task_dialog"] floating enable
for_window [window_role="Preferences"] floating enable
for_window [window_type="dialog"] floating enable
for_window [window_type="menu"] floating enable
for_window [window_role="About"] floating enable
for_window [app_id="xdg-desktop-portal-kde"] floating enable
for_window [app_id="xdg-desktop-portal-gtk"] floating enable

# colour of border, background, text, indicator, and child_border
client.focused              #2f2f2f #000000 #2f2f2f #2f2f2f #2f2f2f
client.focused_inactive     #000000 #000000 #2f2f2f #000000 #000000
client.unfocused            #000000 #000000 #2f2f2f #000000 #000000
client.urgent               #bf616a #000000 #2f2f2f #bf616a #000000
client.placeholder          #000000 #000000 #2f2f2f #000000 #000000
client.background           #000000

floating_minimum_size 75 x 50
focus_on_window_activation urgent
default_border pixel
default_floating_border pixel
hide_edge_borders smart
focus_follows_mouse yes
show_marks no

exec_always --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ mute

include /etc/sway/config.d/*
exec --no-startup-id "systemctl --user start sway-session.target"

exec --no-startup-id $term
exec_always --no-startup-id pidof pms > /dev/null && exit 0 || footclient --app-id=pms pms
exec_always --no-startup-id pidof nnn > /dev/null && exit 0 || footclient --app-id=nnn nnn