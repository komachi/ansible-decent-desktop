{% set pipeline = lookup('file', 'files/firefox/ublock-settings.json') %}
{
  "name": "uBlock0@raymondhill.net",
  "description": "ignored",
  "type": "storage",
  "data": {
    "adminSettings": {{ pipeline | to_json }}
  }
}
