- name: Install bitcoin from flatpak
  flatpak:
    name: org.bitcoincore.bitcoin-qt
    state: present

- name: Ensures ~/.var/app/org.bitcoincore.bitcoin-qt/config/Bitcoin exists
  file:
    path: "/home/{{ username }}/.var/app/org.bitcoincore.bitcoin-qt/config/Bitcoin"
    state: directory
    owner: "{{ username }}"
    group: "{{ username }}"

- name: Configure bitcoind
  template:
    src: 'bitcoin/bitcoin.conf.j2'
    dest: "/home/{{ username }}/.var/app/org.bitcoincore.bitcoin-qt/config/Bitcoin/bitcoin.conf"
    mode: 0640
    owner: "{{ username }}"
    group: "{{ username }}"

- name: Copy blocknotify script
  copy:
    src: 'bitcoin/blocknotify.sh'
    dest: "/home/{{ username }}/.var/app/org.bitcoincore.bitcoin-qt/blocknotify.sh"
    mode: '0740'
    owner: "{{ username }}"
    group: "{{ username }}"

- name: Add systemd service
  copy:
    src: 'systemd/bitcoind.service'
    dest: "/home/{{ username }}/.config/systemd/user/bitcoind.service"
    mode: '0640'
    owner: "{{ username }}"
    group: "{{ username }}"

- name: Disable bitcoind service
  become: yes
  become_user: "{{ username }}"
  systemd:
    scope: user
    name: bitcoind
    enabled: no
    state: stopped